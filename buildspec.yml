version: 0.2

batch:
  fast-fail: false
  build-graph:
    - identifier: rspec
      buildspec: buildspec_test.yml
#    - identifier: docker_build
#      buildspec: build2.yml
#      env:
#        variables:
#          BUILD_ID: build2
#      depend-on:
#        - rspec
